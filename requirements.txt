websockets>=12.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Required only when actually placing orders on Hyperliquid (execute_order).
# Lazy-imported by adapters/hyperliquid.py so read-only sessions skip them.
hyperliquid-python-sdk>=0.10.0
eth-account>=0.13.0
